# Discover reads the local backend API. Android requires adb reverse tcp:3000 tcp:3000
# or a reachable emulator host URL. Default: http://localhost:3000
# EXPO_PUBLIC_API_URL=http://127.0.0.1:3000
# Bundled values are public. Never put provider credentials here.
