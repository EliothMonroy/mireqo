# Event Details and Saved decisions

## Confirmed

- D01: User authorized proceeding with stage 3 (Event Details and Saved Events together) after the next-feature proposal. Existing mock catalog, coverage and editorial visual direction continue.
- D02: User answered “Use this rule (recommended)” to: use known end time for Past; without end time move to Past only after the event-local calendar day ends and label Date passed rather than claiming it ended. Undated events remain in a Date to be announced group within Upcoming.
- D03: Existing architecture requires local SQLite membership/snapshots, cross-screen consistency and offline restart support, without accounts/sync. Existing areas remain Coacalco, Tultitlán and all Mexico City.
- D04: Existing workflow requires durable plans, isolated implementation with no implementer commit/push, independent tester, then independently reviewed successful delivery. User's prior permission for Apple native UI-test tooling remains applicable.

## Routine scoped interpretations

- Implement the two functional primary destinations Discover and Saved now; Search and Settings remain deferred, without dead tabs. Details stays shared above browsing.
- Summary-only saves are permitted and persisted immediately offline. Missing detailed fields are identified; a successful detail fetch enriches snapshots without owning membership.
- Unknown-date ordering is separated rather than assigning a fake instant. Cancellation/postponement is independent of temporal grouping. A missing end time never establishes an ongoing event.
- Use additive details contracts/storage; do not change strict legacy summary shape. Optional multi-session/source-specific modeling remains deferred.
- Optional Similar Events and reminders are excluded. No public event-link domain/hosting or live ticket source is introduced. Mock external fixtures must be clearly labeled demo, never represented as purchasable real events.

- D05 (routine verification fixture choice): https://example.org/ is the controlled external demo destination, labeled Open demo page and disclosed as an example, never Get Tickets or an official event page. Null-link cases omit the action. Shared text preserves demo provenance.

No unresolved product question blocks implementation. Any material new ambiguity returns to the planner/user before dependent implementation.
