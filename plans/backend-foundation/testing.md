# Backend foundation independent testing

Testing passed on 2026-09-07 for AC1–AC10 and tasks BF03–BF08. This is an independent tester result; final feature assessment and delivery belong to the reviewer. No application code was changed by the tester, and no commits or pushes were made.

## Tested state

- Authoritative branch/worktree: `codex/backend-foundation`, `/Users/eliothmonroy/Documents/Github/mireqo/.worktrees/backend-foundation`.
- Base: `main`, `acbb0babd6efd18910bfe36492a92f1ffa7b236d`.
- Implementation includes uncommitted and untracked files; testing inspected that full working state.
- [Code fingerprint](evidence/tested-code.sha256): 49 present, Git tracked or unignored files, excluding Markdown and `plans/`. Manifest SHA-256: `2608c44b523815ade2c9de97b308db9422386ca21dac741697236a39a825ba04`. Run `shasum -a 256 -c plans/backend-foundation/evidence/tested-code.sha256` from the worktree to verify correspondence. Deleted old root mobile files were separately inspected through Git status and compared to their moved counterparts.
- Lockfile SHA-256: `8a61ab41b610496abc9ab3ff043cda193ce6ff4ac61c840ad93ad6cad21e910a`.
- Tester strengthened backend tests to check persisted ordered run outcomes, unique attempt IDs, timestamp values, schema-valid HTTP health/errors and sanitized readiness. Full checks and integration were rerun afterward.

## Automated and service verification

Commands ran sequentially for installation/check phases with `PATH="$HOME/.local/bin:/opt/homebrew/bin:$PATH"` and pinned `mise exec --` tools. Node 24.20.0, pnpm 12.3.4, TypeScript 6.0.3.

| Criteria       | Independent evidence and outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC1            | `pnpm install --frozen-lockfile` passed in authoritative checkout. Copied present tracked/unignored source into fresh `/var/folders/qw/18v8y7cn4wg9tjl1txnqs3cr0000gn/T/mireqo-tester-fresh-uxx7mp1h`; `pnpm --dir <copy> install --frozen-lockfile` and `pnpm --dir <copy> build` passed without existing node_modules. Fresh and authoritative lockfiles remained byte-identical. Logs `/tmp/mireqo-tester-frozen.log`, `-fresh.log`, `-fresh-build.log`.                                                                                                                     |
| AC2, AC5, AC10 | `pnpm check` passed after tester assertions: contracts/backend builds, all package lint/type checks, formatting, seven tooling tests, one contracts test, two backend tests, six mobile tests in two suites. Log `/tmp/mireqo-tester-check.log`. Source inspection confirms platform-neutral contracts and unchanged launch screen without network import.                                                                                                                                                                                                                      |
| AC3, AC4       | Independently reran inspected `/tmp/mireqo-backend-smoke.py` with pinned runtime. Compiled API and worker ran as separate processes; actual HTTP health/ready/OpenAPI returned 200. Both exited successfully within five seconds of SIGTERM. Invalid port configuration and unconfirmed destructive reset exited nonzero. Source inspection confirms shared configuration validation, server close hook, worker timer cancellation and DB teardown. Log `/tmp/mireqo-tester-smoke.log`.                                                                                         |
| AC4, AC5       | HTTP tests assert schema-valid health/readiness/errors, invalid body rejection through test-only route, sanitized unexpected error and readiness bodies, common not-found response, and generated OpenAPI health enum. Database integration asserts successful migrated readiness and bounded unavailable readiness under five seconds while liveness stays 200.                                                                                                                                                                                                                |
| AC6            | Independently exercised actual Compose development stop/start and compared fixture rows before/after; preserved. Pinned official PostGIS container reports PostgreSQL 17.5 / PostGIS 3.5.2, linux/amd64, under `colima-mireqo`. Development volume `mireqo_development`; test service uses separate port/user/database and tmpfs. Guard tests reject development, remote host, wrong database, and URL host override before mutation. Reset command requires explicit destructive flag and targets fixed development service/volume. No destructive development reset executed. |
| AC7, AC8       | `pnpm test:integration` passed against actual test database, after stronger assertions. Drops/recreates only allowlisted test schema, rejects readiness before migrations, applies migration 001, inserts sentinel, upgrades to 002 and repeats migration while preserving sentinel. Repeated seed leaves one deterministic fixture row. Actual PostGIS distance is between 110–112 km and outside 1 km radius. Typed inserts/selects and real timestamp values verified. Development fixture rows unchanged across integration. Log `/tmp/mireqo-tester-integration.log`.      |
| AC9            | Actual DB test verifies four distinct fixture attempts ordered succeeded/succeeded/failed/succeeded with correct error codes and finish timestamps; failed fixture transaction does not increment counter. Separate child process holds source lock: same source skips, different source succeeds. SIGKILL releases lock; next owner reconciles old run to interrupted and succeeds. Default worker source list is empty; live providers are never configured or called.                                                                                                        |
| AC10           | Inspected root/per-package scripts, environment examples, Compose, build guidance and architecture against implementation. `git diff --check` passed. Generated native outputs, dependencies, local environment and build outputs remain ignored. Maintained files contain only explicit local example credentials, with no provider secrets.                                                                                                                                                                                                                                   |

## Current architecture alignment

Independently verified architecture.md describes the implemented TypeBox schemas, Fastify type provider and Swagger generation, dedicated-session advisory locks and interrupted-run reconciliation. Remaining open import decisions concern provider policies and additional production recovery requirements. These statements match the tested implementation. Rechecked all 49 code-manifest entries and `git diff --check`: passed. The documentation-only update leaves runtime, tests and lockfile unchanged; existing execution evidence remains applicable.

## Native evidence and manual visual verification

Independent tester inspected the implementer's successful native build logs and verified installed artifact correspondence instead of duplicating full native compilation. Android command `pnpm android --device Medium_Phone --no-bundler` completed 455 Gradle tasks successfully in 48 seconds; iOS command `pnpm ios --device C588ADDD-FC8A-48E8-BA7D-B14D9093962D --no-bundler` completed successfully with zero errors. Both logs identify this authoritative worktree's `apps/mobile`. Logs: `/tmp/mireqo-backend-android.log`, `/tmp/mireqo-backend-ios.log`.

- Android: emulator-5554, Android 17/API 37. Pulled installed APK and compared SHA-256 to `apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk`: `8e2d185654a6da30a86a08bb7a5e09c6d3ce14c9750466e448bf6dd54b9f706f`.
- iOS: iPhone 17 Pro, iOS 26.5, simulator `C588ADDD-FC8A-48E8-BA7D-B14D9093962D`, Xcode 26.6. Installed `Mireqo` and `Mireqo.debug.dylib` match DerivedData build output byte-for-byte; SHA-256 respectively `8d11ac5c4e02923343df365bbf319f2d7e48a955228e3a41ae6abea03270f0ee` and `61b6dd027a698e6d8eef1eb10b42c0897385018e366e1d881eca367530e9d4dc`.
- Compared moved app.json, route/layout, FoundationScreen and theme byte-for-byte to base revision; unchanged.
- Verified live Metro process 29793 working directory is this worktree's `apps/mobile`. Independently force-stopped/reopened Android and terminated/reopened iOS using the local development-client URL; Android reverse tcp:8081 enabled.
- Inspected newly captured [Android](evidence/tester-android-launch.png) and [iOS](evidence/tester-ios-launch.png) screenshots: both display Mireqo and the unchanged foundation text with no clipping or error UI. Expo development overlay remains visible. This verifies unchanged light appearance; the feature plan explicitly scopes relocation verification to unchanged screen assertion plus actual launch, without adding exhaustive appearance/accessibility testing.
- Requested Metro shared-contract entry bundles independently for both platforms: HTTP 200, Android 955439 bytes / iOS 955431 bytes, each containing the runtime validation implementation. This verifies package resolution without altering launch code.
- Native correspondence/launch log: `/tmp/mireqo-tester-native.log`.

## Scope and handoff inventory

No unverified behavior remains within the foundation acceptance criteria. Fixture tests do not establish live-provider, catalog/search/pagination, release, production deployment, physical-device signing or event completeness behavior; those are outside this feature.

Reviewer input is [reviewer-current.md](reviewer-current.md), this current passing testing record, code fingerprint and launch images, along with current repository specs/guidance. Reviewers should assess source directly, including untracked files. Implementation ownership remains separate from final review.

Resources retained: authoritative feature worktree/branch; helper `codex/backend-mobile` at sibling `.worktrees/backend-mobile`; root approved documentation and original plans preserved; Compose `mireqo-db-1` / `mireqo-test-db-1`, persistent development volume, test tmpfs, Colima `mireqo`; Metro 29793 on 8081 and active simulators; ignored native outputs, root gem bundle, `/tmp/mireqo-tester-*` logs and pulled APK, tester fresh copy above, implementer fresh copy `/var/folders/qw/18v8y7cn4wg9tjl1txnqs3cr0000gn/T/mireqo-backend-fresh-ongq1g8c`, `/tmp/mireqo-backend-*` logs and helper patch. Smoke API/worker processes exited. Colima is the approved shared host runtime. Reviewer cleanup must preserve open PR source, user edits and active resources until safe.
